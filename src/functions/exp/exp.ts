import { Expression } from "../../core/Expression";
import { NatureExponentialExpression } from "./NatureExponentialExpression";

export const exp = (x: Expression) => NatureExponentialExpression.from(x);