{
  "name": "rhq-expressions",
  "version": "1.0.5",
  "description": "rhq's lib for math expressions, capable of constructing math expressions, pieced expressions and derivatives",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "clean": "rm -rf build",
    "build": "npm run clean && tsc && cp ./package.json build",
    "pub": "npm run test && npm run build && npm publish ./build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.4",
    "ts-loader": "^9.2.3",
    "tslib": "^2.1.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@types/lodash": "^4.14.171",
    "lodash": "^4.17.21"
  }
}
